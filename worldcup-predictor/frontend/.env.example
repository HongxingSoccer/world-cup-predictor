# Public envs (exposed to the browser bundle).
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_SHARE_BASE_URL=http://localhost:8080/s/

# Server-only envs.
# (none in Phase 3 — all reads come through the public API.)
