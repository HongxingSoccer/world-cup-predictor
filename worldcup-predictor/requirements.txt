# --- Core ORM / DB ---
SQLAlchemy>=2.0,<2.1
alembic>=1.13
psycopg2-binary>=2.9

# --- Validation & Settings ---
pydantic>=2.0
pydantic-settings>=2.0

# --- Async / Background tasks ---
celery[redis]>=5.3
redis>=5.0

# --- Streaming ---
kafka-python>=2.0
confluent-kafka>=2.3

# --- Scraping & HTTP ---
scrapy>=2.11
beautifulsoup4>=4.12
lxml>=4.9
httpx>=0.26
playwright>=1.40

# --- Utilities ---
python-dotenv>=1.0
structlog>=24.1
tenacity>=8.2
rapidfuzz>=3.6

# --- Phase 2: ML feature engineering / dataframes ---
pandas>=2.0
pyarrow>=14.0
numpy>=1.26
tqdm>=4.66
scipy>=1.11

# --- Phase 2: ML inference API ---
fastapi>=0.110
uvicorn[standard]>=0.27

# --- Phase 2: experiment tracking + reporting ---
mlflow>=2.10,<3
matplotlib>=3.8
jinja2>=3.1
optuna>=3.5

# --- Phase 3: object storage (MinIO / S3) for share cards ---
boto3>=1.34

# --- Phase 4: gradient boosting model + LLM client + push notifications ---
xgboost>=2.0
scikit-learn>=1.3
openai>=1.12
pywebpush>=1.14
cryptography>=42.0

# --- Dev / Test ---
pytest>=7.4
pytest-asyncio>=0.23
pytest-timeout>=2.2
