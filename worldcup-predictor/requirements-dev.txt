# Dev / Test dependencies. Installed in CI on top of requirements.txt;
# NOT included in the production Docker image (the Dockerfile only runs
# `pip install -r requirements.txt`).
#
# Keep this file ordered by category so it diffs nicely.

# --- pytest stack ---
pytest>=7.4
pytest-asyncio>=0.23
pytest-timeout>=2.2

# --- Mocks for tests against backing services ---
fakeredis>=2.20

# --- Static analysis (blocking in CI) ---
ruff>=0.7
mypy>=1.10
