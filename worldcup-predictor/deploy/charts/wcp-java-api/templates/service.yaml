apiVersion: v1
kind: Service
metadata:
  name: {{ include "wcp-java-api.fullname" . }}
  labels:
    {{- include "wcp-java-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wcp-java-api.selectorLabels" . | nindent 4 }}
