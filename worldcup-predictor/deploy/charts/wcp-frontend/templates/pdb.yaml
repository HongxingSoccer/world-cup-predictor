{{- if .Values.podDisruptionBudget.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "wcp-frontend.fullname" . }}
  labels:
    {{- include "wcp-frontend.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "wcp-frontend.selectorLabels" . | nindent 6 }}
{{- end }}
