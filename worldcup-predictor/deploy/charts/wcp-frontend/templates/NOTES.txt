{{ include "wcp-frontend.fullname" . }} installed.

The frontend Service is reachable inside the cluster at:
  http://{{ include "wcp-frontend.fullname" . }}:{{ .Values.service.port }}

Public access is handled by the umbrella chart's ALB Ingress
(deploy/charts/wcp/templates/ingress.yaml), not this sub-chart.

Quick check from another pod in the namespace:
  kubectl run -n {{ .Release.Namespace }} curl --rm -it --image=curlimages/curl \
    --restart=Never -- curl -sf http://{{ include "wcp-frontend.fullname" . }}:{{ .Values.service.port }}/api/health
